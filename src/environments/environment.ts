// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    firebase: {
        apiKey: 'AIzaSyDOwgQLm9E6GB1YvIjH_PivTHPytq1i0j4',
        authDomain: 'inmobiliariasdr.firebaseapp.com',
        databaseURL: 'https://inmobiliariasdr.firebaseio.com',
        projectId: 'inmobiliariasdr',
        storageBucket: 'inmobiliariasdr.appspot.com',
        messagingSenderId: '10316552092',
        appId: '1:10316552092:web:68c992e6a653bdfe'
    }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
